import { Archive, ClipboardCheck, Download, FileDown } from "lucide-react";
import type { DownloadKind } from "../types/api";
import { Button } from "./ui/button";
const config: Record<DownloadKind, { title: string; description: string; icon: typeof FileDown }> = { submission: { title: "Submission", description: "Final ranked candidate CSV", icon: FileDown }, audit: { title: "Audit report", description: "Detailed ranking audit", icon: ClipboardCheck }, all: { title: "All output files", description: "Complete output archive", icon: Archive } };
export function DownloadCard({ kind, loading, onDownload }: { kind: DownloadKind; loading: boolean; onDownload: (kind: DownloadKind) => void }) { const { title, description, icon: Icon } = config[kind]; return <article className="surface flex flex-col p-5"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-indigo-50 text-indigo-600"><Icon className="h-5 w-5" /></div><h3 className="mt-4 font-bold text-[#172554]">{title}</h3><p className="mt-1 flex-1 text-sm leading-6 text-slate-500">{description}</p><Button variant="outline" className="mt-5 w-full" loading={loading} onClick={() => onDownload(kind)}><Download className="h-4 w-4" />Download</Button></article>; }
