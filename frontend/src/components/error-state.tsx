import { AlertTriangle, RotateCcw } from "lucide-react";
import { Button } from "./ui/button";
export function ErrorState({ title = "Unable to reach the backend", error, onRetry }: { title?: string; error?: unknown; onRetry?: () => void }) { const message = error instanceof Error ? error.message : "Please check your connection and try again."; return <div className="surface flex min-h-52 flex-col items-center justify-center px-6 text-center"><div className="mb-4 rounded-2xl bg-rose-50 p-3 text-rose-600"><AlertTriangle className="h-6 w-6" /></div><h2 className="text-lg font-bold text-[#172554]">{title}</h2><p className="mt-2 max-w-xl text-sm leading-6 text-slate-500">{message}</p>{onRetry ? <Button variant="outline" className="mt-5" onClick={onRetry}><RotateCcw className="h-4 w-4" />Retry</Button> : null}</div>; }
