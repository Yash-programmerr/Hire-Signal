import { Search, X } from "lucide-react";
export function SearchBar({ value, onChange }: { value: string; onChange: (value: string) => void }) { return <label className="relative block"><span className="sr-only">Search candidates</span><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" /><input value={value} onChange={(event) => onChange(event.target.value)} placeholder="Search candidates" className="focus-ring h-10 w-full rounded-xl border border-slate-200 bg-white pl-9 pr-9 text-sm text-slate-700 shadow-sm placeholder:text-slate-400 sm:w-64" />{value ? <button aria-label="Clear search" onClick={() => onChange("")} className="focus-ring absolute right-2 top-1/2 -translate-y-1/2 rounded p-1 text-slate-400"><X className="h-3.5 w-3.5" /></button> : null}</label>; }
